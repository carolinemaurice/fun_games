<h1>Book this game</h1>
<%= simple_form_for [@game, @booking] do |f| %>
  <%= f.input :start_date, as: :date%>
  <%= f.input :end_date, as: :date%>
  <%= f.submit "Confirm booking", class: "btn btn-primary" %>
<% end %>
